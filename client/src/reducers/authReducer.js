const  initialState = {
    state: null,
    authenticated: null
}

export default function (state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}